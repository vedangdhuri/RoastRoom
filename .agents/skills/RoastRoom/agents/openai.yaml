interface:
  display_name: "RoastRoom"
  short_description: "Repo-specific patterns and workflows for RoastRoom"
  default_prompt: "Use the RoastRoom repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true